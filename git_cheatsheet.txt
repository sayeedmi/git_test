Command related to a remote repository:
	1. git clone <SSH URL>
	2. git push OR git push origin main

Command related to the workflow:
	1. git add .
	2. git commit -m "A message describing what you have done to make this snapshot different"

Command related to checking status or log history:
	1. git status
	2. git log

